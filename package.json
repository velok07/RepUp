{
  "name": "repup-root",
  "private": true,
  "scripts": {
    "build:backend": "npm --prefix backend ci && npm --prefix backend run prisma:generate && npm --prefix backend run build",
    "serve:backend": "node backend/dist/server.js"
  }
}
