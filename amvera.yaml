meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: |
    cd backend
    npm install
    npx prisma generate
    npm run build

run:
  command: cd backend && npm run start
  containerPort: 4000
  servicePort: 80